# Security Spec

## Invariants
1. Any authenticated user can create a room.
2. Any authenticated user can read a room if they have the ID.
3. Any authenticated user can add blocks to any room they know the ID of.
4. Users can only delete blocks they created, or the room host can delete any block.

## Payloads
- TBD
